{{#each notes}}
  <div class="card card-body mb-2">
    <h4>{{title}}</h4>
    <p>{{details}}</p>
    <a href="/notes/edit/{{id}}" class="btn btn-dark btn-block">Edit</a>
  </div>
{{else}}
  <p>No notes</p>
{{/each}}